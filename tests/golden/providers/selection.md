# Provider selection, worked cases

Generated by `scripts/gen_provider_goldens.py`; do not edit by hand.

Each case is one requirement and one offer. The plan under it is what `scripts/providers.py` selects, in the order it ranks them, with every refusal named. Registry: `vibecheck.provider_registry` 1.0.0.

## A review that has been given the source and nothing else

The safe default. Every live method needs a grant nobody has given yet, so the plan falls all the way to reading the source — and says so, instead of implying the reading settled anything.

```text
Requirement: vibecheck.control.authz.object_level in private_test, 4 cell(s).
n/a       vibecheck Supabase live probe — vibecheck Supabase live probe observes supabase_project, which this review does not have.
n/a       Playwright two-account flow — Playwright two-account flow observes deployed_web_app, which this review does not have.
n/a       Guided two-account browser test — Guided two-account browser test observes deployed_web_app, which this review does not have.
SELECTED  Code and policy review — indicative; material only, fills no coverage cell and closes nothing
n/a       vibecheck SQL migration analysis — vibecheck SQL migration analysis observes sql_migrations, which this review does not have.
unused    vibecheck.sh static scanner — eligible, but a provider already in the plan covers everything it would have
Coverage: 0 of 4 requested cell(s); this plan closes no control.
GAP       no available provider observes user_owned_record / other_account / create
GAP       no available provider observes user_owned_record / other_account / delete
GAP       no available provider observes user_owned_record / other_account / read
GAP       no available provider observes user_owned_record / other_account / update
```

## The Supabase probe is authorized, read-only

The strongest applicable method runs, and covers exactly one of the four requested cells. The other three are not quietly dropped: they are gaps with the grant that would reach them.

```text
Requirement: vibecheck.control.authz.object_level in private_test, 4 cell(s).
SELECTED  vibecheck Supabase live probe — decisive; covers 1 requested cell(s)
EXCLUDED  Playwright two-account flow — Playwright two-account flow is not bundled and needs node, playwright, which is not installed here.
unused    Guided two-account browser test — eligible, but a provider already in the plan covers everything it would have
SELECTED  Code and policy review — indicative; material only, fills no coverage cell and closes nothing
n/a       vibecheck SQL migration analysis — vibecheck SQL migration analysis observes sql_migrations, which this review does not have.
unused    vibecheck.sh static scanner — eligible, but a provider already in the plan covers everything it would have
Coverage: 1 of 4 requested cell(s); this plan closes no control.
GAP       no available provider observes user_owned_record / other_account / create
GAP       no available provider observes user_owned_record / other_account / delete
GAP       no available provider observes user_owned_record / other_account / update
```

Authorization:

- **required_to_run** — vibecheck Supabase live probe must be supplied with the listed credentials; allowed to send data to the listed destinations; allowed to make network requests to the target before this step runs.
- **would_strengthen_the_plan** — Playwright two-account flow is available but unused: authorize prov-playwright-two-account; install node, playwright.

## The anonymous matrix, with writing withheld

The same provider covers the read cell and is refused the create cell, because covering it means inserting a row. A side effect excludes the aspect that needs it, not the provider.

```text
Requirement: vibecheck.control.authz.anon_data_access in private_test, 2 cell(s).
SELECTED  vibecheck Supabase live probe — decisive; covers 1 requested cell(s)
n/a       Playwright two-account flow — Playwright two-account flow observes deployed_web_app, which this review does not have.
n/a       Guided two-account browser test — Guided two-account browser test observes deployed_web_app, which this review does not have.
SELECTED  Code and policy review — indicative; material only, fills no coverage cell and closes nothing
n/a       vibecheck SQL migration analysis — vibecheck SQL migration analysis observes sql_migrations, which this review does not have.
unused    vibecheck.sh static scanner — eligible, but a provider already in the plan covers everything it would have
Coverage: 1 of 2 requested cell(s); this plan closes no control.
GAP       vibecheck Supabase live probe could observe 1 more cell(s): observing this cell needs write, which was not authorized.
GAP       no available provider observes user_owned_record / anonymous / create
```

Authorization:

- **required_to_run** — vibecheck Supabase live probe must be supplied with the listed credentials; allowed to send data to the listed destinations; allowed to make network requests to the target before this step runs.
- **would_strengthen_the_plan** — vibecheck Supabase live probe is available but unused: authorize write.

## The anonymous matrix, with writing authorized

The same requirement and the same provider, one grant later. This is the whole point of recording effects: the difference between the two plans is a decision somebody made, and it is visible.

```text
Requirement: vibecheck.control.authz.anon_data_access in private_test, 2 cell(s).
SELECTED  vibecheck Supabase live probe — decisive; covers 2 requested cell(s)
n/a       Playwright two-account flow — Playwright two-account flow observes deployed_web_app, which this review does not have.
n/a       Guided two-account browser test — Guided two-account browser test observes deployed_web_app, which this review does not have.
unused    Code and policy review — eligible, but a provider already in the plan covers everything it would have
n/a       vibecheck SQL migration analysis — vibecheck SQL migration analysis observes sql_migrations, which this review does not have.
unused    vibecheck.sh static scanner — eligible, but a provider already in the plan covers everything it would have
Coverage: 2 of 2 requested cell(s); this plan closes no control.
```

Authorization:

- **required_to_run** — vibecheck Supabase live probe must be supplied with the listed credentials; allowed to send data to the listed destinations; allowed to make network requests to the target; allowed to write to the target system before this step runs.

## No Supabase project, no Playwright, one available person

The two automated methods are gone for different reasons — one has nothing to observe, the other is not installed — and only the second is a gap. A person with a browser is what is left, and it is a real observation, not a consolation.

```text
Requirement: vibecheck.control.authz.object_level in public_release, 1 cell(s).
n/a       vibecheck Supabase live probe — vibecheck Supabase live probe observes supabase_project, which this review does not have.
EXCLUDED  Playwright two-account flow — Playwright two-account flow is not bundled and needs node, playwright, which is not installed here.
SELECTED  Guided two-account browser test — decisive; covers 1 requested cell(s)
unused    Code and policy review — eligible, but a provider already in the plan covers everything it would have
n/a       vibecheck SQL migration analysis — vibecheck SQL migration analysis observes sql_migrations, which this review does not have.
unused    vibecheck.sh static scanner — eligible, but a provider already in the plan covers everything it would have
Coverage: 1 of 1 requested cell(s); this plan closes no control.
```

Authorization:

- **required_to_run** — Guided two-account browser test must be supplied with the listed credentials; allowed to send data to the listed destinations; allowed to make network requests to the target before this step runs.
- **would_strengthen_the_plan** — Playwright two-account flow is available but unused: install node, playwright.

## Playwright is installed and authorized

The declared fallback order decides between two methods of equal strength, and the cheapest one still goes first. What Playwright adds is the cells the probe cannot reach at all.

```text
Requirement: vibecheck.control.authz.object_level in private_test, 4 cell(s).
SELECTED  vibecheck Supabase live probe — decisive; covers 1 requested cell(s)
SELECTED  Playwright two-account flow — decisive; covers 2 requested cell(s)
EXCLUDED  Guided two-account browser test — Guided two-account browser test declares that it requires authorization, and this run was not authorized to use it.
SELECTED  Code and policy review — indicative; material only, fills no coverage cell and closes nothing
n/a       vibecheck SQL migration analysis — vibecheck SQL migration analysis observes sql_migrations, which this review does not have.
unused    vibecheck.sh static scanner — eligible, but a provider already in the plan covers everything it would have
Coverage: 3 of 4 requested cell(s); this plan closes no control.
GAP       Playwright two-account flow could observe 1 more cell(s): observing this cell needs destructive, which was not authorized.
GAP       no available provider observes user_owned_record / other_account / delete
```

Authorization:

- **required_to_run** — vibecheck Supabase live probe must be supplied with the listed credentials; allowed to send data to the listed destinations; allowed to make network requests to the target before this step runs.
- **required_to_run** — Playwright two-account flow must be supplied with the listed credentials; allowed to send data to the listed destinations; allowed to make network requests to the target; allowed to write to the target system before this step runs.
- **would_strengthen_the_plan** — Playwright two-account flow is available but unused: authorize destructive.
- **would_strengthen_the_plan** — Guided two-account browser test is available but unused: authorize prov-guided-browser-test.
